[package]
name = "slct"
version = "0.3.0"
license = "0BSD"
edition = "2024"
publish = false

[dependencies]
argh = { version = "0.1.14", default-features = false, features = ["help"] }
rapidhash = { version = "4.4.1", default-features = false, features = ["std"] }
